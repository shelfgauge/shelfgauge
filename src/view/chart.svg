<svg width="<%= width %>" height="<%= height %>">
  <% lines.forEach((line) => { %>
    <polyline fill="none" stroke="black"
     points="<%= line.map(point => point.join(",")).join(" ") %>"/>
  <% }) %>
</svg>
